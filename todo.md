1. markdown should support list and order list 
2. the positiong of the modal for editing seems random - have it 
3. see if we can have the seed be more dynamic - having the template containing the structure of the template is really annouying it be nice if we could just have it live in js or something so it's easier to edit - or even better yet just auto generate based off the content that is on the page.
4. upload json of data / api for adding/editing pages quickly /easily (enabled ai ) - and get so we can get the whole page data
